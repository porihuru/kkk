# CSV・SharePoint互換性点検（2026-09-13）

結論：完全互換ではありません。CSVは画面操作・HTML生成の開発用として使えますが、SharePointに切り替えた際の保存・再読込の動作を保証しません。
本点検はコードとローカルCSV、模擬REST応答による検証です。実際のSharePointリストは変更していません。

## 未解消の重要な差

追記：投稿者制限の実装時に、公告とリンクの新規保存応答からサーバーIDを`Id`へ保持する処理を追加しました。以下のID問題は一部改善していますが、基準日のID、保存中の再操作、仮IDと公開用IDの統一は引き続き未解消です。投稿者制限の範囲は `OWNERSHIP.md` を参照してください。

| 優先度 | 対象 | 現在の動作と影響 | 対応に必要なこと |
|---|---|---|---|
| 最優先 | 本リストと予定リスト | CSVは公告2組・リンク2組・設定の計5ファイル。SharePointは公告・リンク・設定の3リストだけを読み込み、画面は取得した公告をすべて予定へ入れる。本リスト用の戻り値がなく、本リストは空になる。 | SharePointで本／予定を分ける列、または別リストの実構成を確定し、読み書き両方に反映する。Statusは業務状態であり、勝手に分類キーへ流用しない。 |
| 最優先 | 新規作成後のID | `persistAnnouncement`はサーバーから返る公告・リンクIDを画面データへ反映していない。新規作成後の再編集・削除でローカル仮IDを使い、誤った項目へ操作する可能性がある。基準日の新規作成にも同じ問題がある。 | サーバーIDを保持し、親子参照・画面キー・選択PDFの参照を一貫して更新する。保存中の再操作も制御する。 |
| 最優先 | リスト間移動・差し戻し | `moveAnnouncement`は配列を移動し、IDを作り直すだけ。SharePointへの永続化処理がない。 | 本／予定の保存方式に合わせて移動処理を実装し、再読込後も維持されることを検証する。 |
| 高 | 保存結果・失敗 | 公告とリンク・PDFを複数要求で保存するが、全要求の完了前に成功表示する。削除は画面を先に更新し、一部の失敗を無視する。CSVでは通信失敗がないため発見できない。 | 全処理の完了を待ち、部分失敗を表示して再取得・再試行する。 |
| 高 | 並び順 | `moveAnnouncementOrder`は配列順だけを変え、Sortを更新・保存しない。CSV出力でも古いSortが残り、SharePointでも再読込で戻る。 | Sortを全対象に付け直して保存し、読込時の順序を統一する。 |
| 高 | HTML取り込み | `replaceImportedData`はメモリだけを置換し、SharePointへ同期しない。表示をCSVモードと書き換えてもDataServiceのモードは変わらない。HTML由来のIDで後からSharePointを編集する危険がある。 | 公開HTMLを正本とする取り込みを、SharePointのIDへ対応付けて保存する。未同期状態を明示する。 |
| 高 | 列型 | CSVの値は文字列。SharePoint側は列型を取得するものの、型に応じた入出力変換はない。URL列・Lookup列・日時列などを使用している場合の互換性は未検証。 | 実際のInternalName、TypeAsString、必須・選択肢・Lookup参照先を確認する。OperationDateは現在文字列で扱う。 |
| 中 | PDF | CSVモードではアップロードしない。PDFは選択中のFileとしてメモリに保持するだけ。CSVにバイナリは保存されない。 | 模擬アップロードの成功／失敗を試験し、接続可能になったらフォルダー・権限・実アップロードを確認する。 |
| 中 | 保存寿命 | CSVの追加・修正・削除は画面メモリだけ。再読込・DB切替でCSVを再取得し変更が失われる。SharePointはサーバーへ即時要求する。 | CSV出力による保存と、通信を模擬するテストを区別する。 |
| 中 | 同時更新 | SharePoint更新・削除はIF-MATCHが常にワイルドカード。CSVでは競合を再現しない。 | 複数人編集が必要な場合、ETag競合を検出する。公開HTML更新中の単独作業という前提とは別に、リスト編集の運用を確認する。 |

## 今回修正した不具合

1. CSV自身の出力につくUTF-8 BOMが、再読込時にID列名へ混入する問題を修正。
2. CSV出力名をDB別の読込設定に統一。本リストの出力を予定CSVへ置き戻してしまう名前の逆転を修正。
3. SharePointの`operationdate`を画面用`OperationDate`へ正規化するマッピングを追加。
4. SharePointの`d.__next`を追跡し、2ページ目以降も読み込むよう修正。途中で失敗した場合は不完全なデータを成功として返さない。

## 検証

`node --test tests/data-compat.test.cjs`：5テスト成功。

- 4DB×予定／本のCSV内でIDが重複せず、PDFリンクの親公告が存在すること。
- BOM・日本語・引用符・カンマ・改行・空欄・0を含むCSV往復。
- 4DBの5種類の出力名が読み込み設定と一致すること。
- SharePoint模擬応答の複数ページ、ID、親ID、操作日時の読み込み。
- 2ページ目の失敗時に部分データを成功扱いしないこと。

これらは上表の未解消事項を解決したことを意味しません。特に新規保存後の再編集／削除と本・予定の永続化は、SharePoint運用開始前の必須対応です。
実接続後は認証、権限、列型・内部名、必須列、選択肢、PDF保存先と、保存後の再読込を確認する必要があります。

公開サイトのHTMLが正本という前提は変わりません。CSVまたはSharePointにデータがあることだけで、公開サイトと一致したと判断する機能はまだありません。
