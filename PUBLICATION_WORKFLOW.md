# 掲載終了・結果掲載の更新手順

公開中の公告と更新依頼を別の行で保持します。公開サイトが正本です。入札日到来や結果PDFの登録だけでは公開済みにしません。

## 操作

1. 公告リストの自分の案件で「更新依頼」を押します。管理者はすべての案件を対象にできます。
2. 「掲載終了依頼」または「結果登録」を選択します。結果登録にはPDFファイルが必要です。日本語のPDF名も使用できます。登録時に時刻と依頼IDを含む保存名を自動で付け、既存の公告PDFを上書きしません。
3. 公告予定リストに元公告ID付きの依頼が表示されます。自分の未承認依頼は修正・削除できます。同じ公告の未完了依頼は1件までです。
4. 管理者が確認して「公開待ちにする」を押します。元の公告の公開状態はまだ変わりません。解除して再編集することもできます。
5. HTML出力・公開ページプレビューには公開待ちの変更が反映されます。掲載終了では共通の掲載終了PDF、結果登録では結果PDFにリンクを切り替えます。元の公告リンクはCSV／リスト側に保持します。
6. 通常の方法でHTMLとPDFをWebサーバーへ配置します。このアプリからインターネットへのアップロードは実行しません。
7. 公開サイトからHTMLを取得し、管理者の「公開後の反映確認」で選びます。公告内容・順序・PDFリンクが公開予定全体と一致すると、公開状態を更新し、依頼を反映確認済みにします。不一致・解析失敗は未確認のままです。

掲載終了を経由せず、公告掲載中から直接結果を登録・公開できます。
入札日到来の判定は日本時間の当日0時です。「掲載終了の確認対象」と表示するだけで自動差し替えはしません。
掲載終了PDFは `config/config.txt` の `ENDED_PDF_URL` で変更できます。

## CSVで確認する状況

4DB共通で、本リスト8件・依頼4件を収録しています。すべて動作確認用の架空データです。

| 公告ID | 状況 | 依頼ID |
|---|---|---|
| 1 | 入札日前・公告掲載中（2026-10-01入札） | なし |
| 2 | 入札日到来・掲載終了の確認対象 | なし |
| 3 | 掲載終了依頼済み・未承認 | 101 |
| 4 | 掲載終了済み・結果未登録 | なし |
| 5 | 掲載終了後の結果PDF登録・未承認 | 102 |
| 6 | 公告から直接結果掲載へ・公開待ち | 103 |
| 7 | 結果掲載中・反映確認済み | 104 |
| 8 | 別の投稿者の案件・編集権限確認用 | なし |

基準日は2026-09-13です。日付到来表示は実行日の日本時間で変わります。
`csv-user-1`がID1～7の投稿者、`csv-user-2`がID8の投稿者です。
`demo-*.pdf`は状態表示用のサンプルリンクで、実ファイルは含みません。結果PDFの登録・ZIP出力を試す場合は、依頼の公開待ちを解除し、手元のテストPDFを選んでください。
CSV保存には予定・本リストの公告CSV、リンクCSV、設定CSVの出力が必要です。状態・元公告ID・結果PDFのURL・反映確認日時も公告CSVに含みます。
再読込やDB切替で未出力のメモリ上の変更と選択ファイルは失われます。PDFバイナリはCSVに入りません。結果PDFが手元にない場合はZIP出力を止めます。

## ZIP作成時の公開済み更新（2026-09-13変更）

現在はZIP作成とダウンロード開始が成功した時点で、含まれる公告を反映済み、更新依頼を「公開済」にします。新規公告の本リスト移動は「公開待ち」です。HTML単体出力・プレビューでは状態を変更しません。公開HTMLとの照合は公開後の一致確認として行い、公開済み依頼を「反映確認済み」にします。

保存失敗時は未保存警告を表示します。通常公告・リンク・PDF・移動・並べ替え・削除・ZIP作成後の記録は、失敗した手順から再保存できます。権限・列定義・実環境試験はREADMEの4.2節を参照してください。

## SharePointへの接続準備

公告リストに以下の内部名のテキスト列が必要です。実リストは未変更・未接続です。

| アプリ項目 | 内部名 | 用途 |
|---|---|---|
| ListKind | listkind | `published`＝本リスト、`planned`＝予定・依頼 |
| PublicState | publicstate | 公告掲載中／掲載終了／結果掲載中 |
| TargetID | targetid | 元公告のSharePoint ID |
| RequestType | requesttype | 掲載終了依頼／結果登録 |
| RequestStatus | requeststatus | 依頼の種類／公開待ち／公開済／反映確認済み |
| ResultURL | resulturl | 公開する結果PDFの相対URL |
| ResultName | resultname | 登録時の結果PDF名 |
| VerifiedAt | verifiedat | 照合成功日時（ISO 8601） |

作成者・投稿日時はSharePoint標準のAuthor・Createdを使用します。既存データのListKindが空欄の場合は従来どおり予定側に読み込みます。本リストへ移行する行は、公開HTMLとの対応を確認してListKindとPublicStateを設定してください。
本リストと予定のリンクは親公告IDで分けます。結果PDFの保存後、依頼を保存します。承認・照合結果もSharePointへ保存し、要求失敗時は成功表示しません。
複数項目の保存はトランザクションではありません。反映記録の途中で失敗した場合は再読込して再照合してください。投稿者・管理者のサーバー権限は `OWNERSHIP.md` の設定が別途必要です。

## 検証範囲と制限

18件の自動テストで、日付境界、元リンク保持、結果の直接掲載、未承認除外、重複・不正依頼、照合差分、CSV参照整合性、投稿者権限を確認しています。現行EdgeでHTML生成→解析の一致と一般投稿者の画面を確認しました。
照合は選択されたHTMLの内容を検証します。そのファイルが本当に公開サイトから取得されたか、PDFの中身が正しいかは検証できません。
既存のHTMLテンプレートは物件・役務用です。工事・OP・公募の実際の公開HTML構造への対応と実サイトでの照合は未検証です。4DBのCSVは同じ更新状態の画面・ロジック確認に使用できます。
既存の新規公告の本リスト移動・差し戻しは従来処理です。この更新依頼フローは既存の公開公告を対象とします。
