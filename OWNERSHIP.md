# 公告予定リストの投稿者制限

一般投稿者は自分が作成した予定だけを修正・削除できます。管理者モードでは全投稿が対象です。
画面のボタン表示、編集開始、保存実行、削除実行で同じ判定を使用します。
本リストは従来どおり管理者が操作します。

## CSVでの開発

`config/config.txt` の `CSV_USER_ID` が仮ユーザーID、`CSV_USER_NAME` が表示名です。
新規投稿には `AuthorId` が記録され、公告CSVの出力にも含まれます。
投稿者名は`AuthorName`、投稿日時は`Created`に記録し、CSV出力にも含めます。予定リストには投稿者と投稿日時（日本時間）を表示します。編集時には投稿情報を変更せず、操作日時だけを更新します。SharePointでは標準のAuthor/TitleとCreatedを読み込みます。記録のない既存データは名前をID表示、日時を「不明」とします（IDもなければ投稿者も「不明」）。
ユーザーIDを変更して再読込すると、別ユーザーとしての判定を試せます。
ただしCSVモードの編集はメモリ上だけなので、必要なデータをCSV出力してから切り替えてください。
既存CSVにAuthorIdがない投稿は所有者不明とし、管理者だけが修正・削除できます。既存データに所有者を推測で割り当てていません。
CSVの仮ユーザーと既存の管理者パスワードは開発用です。本人認証やサーバー上のアクセス制御にはなりません。

## SharePoint接続時

`/_api/web/currentuser` から実際のユーザーIDを取得し、公告の標準作成者 `AuthorId` と照合します。
作成者IDをPOSTで指定せず、SharePointが記録する作成者を使用します。
ユーザー確認に失敗した場合は投稿の作成・修正・削除を許可しません。
管理者モードは取得した `IsSiteAdmin` がtrueの場合に利用できます。CSVのパスワードだけではSharePointの管理者になれません。
サイト所有者グループ等を管理者として追加する場合は、実際のグループ／権限の対応を別途設定する必要があります。

## サーバー側の制限は接続後に設定・検証が必要

このリポジトリで実装したものはブラウザー内の操作制限です。SharePointへ接続できないため、実リストの権限は変更していません。
APIを直接呼んでも他人の投稿を変更できないようにするには、SharePoint側で投稿者本人だけが編集できるアイテム権限を設定する必要があります。
公告だけでなく子リンクとPDFにも親公告に対応した権限が必要です。管理者が追加・修正したリンクの作成者と親公告の作成者が異なる場合も考慮してください。
本番前に、投稿者A/B・管理者の3ユーザーで、一覧表示、修正、削除、子リンク、PDF、直接API要求を確認してください。
公開HTMLを正本とする同期や予定／本リストの永続化は `DATA_COMPATIBILITY_AUDIT.md` の未解消事項です。

検証コマンド：`node --test tests/ownership.test.cjs tests/data-compat.test.cjs`
